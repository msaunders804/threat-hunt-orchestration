# ── Core service ─────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# ── Orchestration / agents ───────────────────────────────────────────────
langgraph>=1.2.0
deepagents>=0.6.0
langchain>=1.3.2
langchain-core>=1.4.0

# ── Model providers ──────────────────────────────────────────────────────
# anthropic is the default provider. bedrock (langchain-aws) and ollama
# (langchain-ollama) are lazy-imported by orchestrator/llm.py and only needed
# when INFERENCE_PROVIDER selects them — keep them installed for full coverage.
anthropic>=0.40.0
langchain-anthropic>=1.4.3
langchain-ollama>=0.2.0
langchain-aws>=0.2.0          # Bedrock (GovCloud). Pulls boto3.

# ── Data / detection back ends (Phases 1–2) ──────────────────────────────
elasticsearch>=8.0.0          # LiveESClient; mock mode needs no server
pysigma>=0.11.0               # Sigma rule authoring/validation
pysigma-backend-elasticsearch>=1.0.0   # compile Sigma -> ES query
